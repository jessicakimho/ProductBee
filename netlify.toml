[build]
  command = "npm run build"
  # Base directory should be empty if ProductBee is the repository root
  # If your repo structure is different, adjust accordingly

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"

